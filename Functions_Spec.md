# 功能規格書

## 使用者端

** 針對行動裝置體驗進行最佳化。 **

### 註冊
- 目標：如果使用者尚未註冊，使用 LINE 帳號進行註冊。
- 輸入：無
- 輸出：綁定結果。
- 流程細節：
    - 使用者存取註冊頁面。
    - 使用者點擊註冊按鈕。
    - 使用者被導向至 LINE 登入頁面。
    - 使用者使用 LINE 帳號登入。
    - 使用者被導向回註冊頁面。
    - 使用者點擊註冊按鈕。
    - 使用者被導向至首頁。
    - 使用者完成註冊。
- 備註：
    - 關於綁定流程，請參考 `./try_line_connect_page/main.py`。
    - **綁定需經管理員審核**：使用者送出綁定申請後，需由管理員於後台審核通過後方可查看奉獻紀錄。
    - 如果使用者已註冊且已通過審核，導向至首頁。
    - 如果使用者已註冊但尚未審核，顯示「綁定申請中」訊息。
    - 如果使用者未登入，導向至 LINE 登入頁面。

### 登入
- 目標：如果使用者尚未登入，使用 LINE 帳號登入。
- 輸入：無
- 輸出：登入結果。
- 流程細節：
    - 使用者存取登入頁面。
    - 使用者點擊登入按鈕。
    - 使用者被導向至 LINE 登入頁面。
    - 使用者使用 LINE 帳號登入。
    - 使用者被導向至首頁。
    - 使用者已登入。
- 備註：
    - 關於登入流程，請參考 `./try_line_connect_page/main.py`。
    - 如果使用者已登入，導向至首頁。
    - 如果使用者未登入，導向至 LINE 登入頁面。

### 自動登入
- 目標：如果使用者處於已登入的工作階段，則導向至首頁。
- 輸入：無
- 輸出：登入結果。
- 流程細節：
    - 使用者存取登入頁面。
    - 使用者被導向至首頁。
    - 使用者已登入。
- 備註：
    - 關於自動登入流程，請參考 `./try_line_connect_page/main.py`。
    
### 首頁
- 目標：使用者奉獻紀錄的互動式儀表板。
- 輸入：無
- 輸出：使用者奉獻紀錄的儀表板。
- 頁面細節：
    - 標頭顯示姓名和會員 ID。
    - 副標題顯示資料時間範圍。
    - 強調文字顯示關鍵奉獻數據：
        - 總奉獻金額。（旁邊設有「眼睛」圖示開關，預設為隱藏數值 `****`，點擊後顯示數字，保護隱私）
        - 總奉獻次數。
    - 甜甜圈圖顯示總奉獻金額的比例，按不同的奉獻類別分組。
        - 在圖表上顯示類別名稱。
        - 當使用者長按時強調該類別，顯示類別、總奉獻金額比例和奉獻金額。
    - 堆疊長條圖顯示奉獻金額，按不同的奉獻類別分組，數據按日期分組。
        - 在圖表上顯示日期。
        - 當使用者長按時強調該長條，顯示日期、總奉獻金額比例和奉獻金額。
    - 奉獻詳細資料表格，從最新到最舊排序。
        - 表格範例：
            | 日期 | 類別 | 金額 |
            |---|---|---|
            | 2025-01-01 | 月定奉獻 | 100 |
            | 2025-01-02 | 感恩奉獻 | 200 |
            | 2025-01-03 | 感恩節奉獻 | 300 |
        - 點擊標頭時，表格將根據該標頭排序，再次點擊反轉順序。
        - 表格上方有一個篩選區塊，包含 2 個篩選器：
            - 日期範圍篩選器。
            - 類別篩選器。
        - 每頁顯示 10 筆奉獻詳細資料，表格下方有一個分頁區塊，包含 2 個按鈕：
            - 上一頁按鈕。
            - 下一頁按鈕。
           
## 管理員端

** 針對桌面裝置體驗進行最佳化。 **

### 登入
- 目標：如果使用者尚未登入，使用帳號和密碼登入。
- 輸入：帳號、密碼
- 輸出：管理員儀表板。
- 流程細節：
    - 使用者存取管理員登入頁面。
    - 使用者輸入帳號和密碼，然後按下登入按鈕。
    - 使用者被導向至管理員儀表板。
- 備註：
    - 如果使用者已登入，導向至管理員儀表板。
    - 如果使用者未登入，導向至管理員登入頁面。

### 登出
- 目標：登出使用者。
- 輸入：無
- 輸出：登入頁面。
- 流程細節：
    - 使用者按下登出按鈕。
    - 使用者被導向至登入頁面。
    - 使用者已登出。

### 管理員標頭
- 目標：所有管理員頁面的管理員標頭。
- 輸入：無
- 輸出：導覽列。
- 頁面細節：
    - 標頭顯示教會名稱。
    - 副標題顯示目前時間。
    - 登出按鈕。
    - 功能導覽：
        - 儀表板 (包含待審核申請通知)
        - 上傳資料
        - 會友管理 (包含審核綁定功能)
        - 奉獻類別管理
        - 教會管理（保留功能）

### 管理員儀表板
- 目標：供管理員管理使用者和奉獻資料的儀表板。
- 輸入：無
- 輸出：管理員儀表板。
- 頁面細節：
    - 關鍵數據顯示：
        - 總奉獻金額 (NT$)。
        - 總奉獻筆數。
        - **待審核綁定申請筆數**：若有待審核申請，系統將跳出提醒視窗。
    - 甜甜圈圖顯示已記錄的奉獻類別分佈比例。
    - 奉獻詳細資料表格 (尚未實作完整篩選，目前僅為總覽)。
        - 點擊標頭時，表格將根據該標頭排序，再次點擊反轉順序。
        - 表格上方有一個篩選區塊，包含 4 個篩選器：
            - 日期範圍篩選器。
            - 類別篩選器。
            - 會友編號篩選器。
            - 會友姓名篩選器。
        - 每頁顯示 10 筆奉獻詳細資料，表格下方有一個分頁區塊，包含 2 個按鈕：
            - 上一頁按鈕。
            - 下一頁按鈕。
    
### 管理員上傳資料
- 目標：供管理員上傳奉獻資料。
- 輸入：無
- 輸出：管理員上傳資料頁面。
- 頁面細節：
    - 上傳區塊，用於上傳奉獻資料 Excel 檔案或 CSV 檔案。
    - 提交表單按鈕。
    - 取消表單按鈕。
- 流程細節：
    - 使用者存取管理員上傳資料頁面。
    - 透過瀏覽檔案或拖放檔案上傳 Excel 檔案或 CSV 檔案。
    - 系統進行初步格式檢查（例如：必要欄位是否存在、格式是否正確）。
    - 使用者按下提交按鈕。
    - 將跳出確認彈窗，詢問使用者是否確認上傳。
        - 如果使用者確認，系統將開始上傳資料。
        - 採用「整批交易」機制，若檔案中包含錯誤資料（如格式錯誤），則整批上傳失敗，並回傳錯誤行數與原因供修正。
        - 如果使用者取消，系統將導向至管理員上傳資料頁面。
    - 上傳成功後，將跳出成功彈窗，顯示已上傳多少筆記錄。按下確定按鈕導向至管理員儀表板。
    - **個資保護與隱碼規則**：
        - 會友姓名隱碼：上傳會友資料時，系統將自動針對 `Name` 欄位進行隱碼處理。
            - 2 個字的姓名：隱碼第 2 個字 (例：王小 -> 王O)。
            - 3 個字以上的姓名：保留第一個與最後一個字，中間全部隱碼 (例：王小明 -> 王O明、歐陽龍 -> 歐OO龍)。
        - 奉獻明細最小化：`Devotions` 集合中不再儲存 `MemberName`，僅保留 `MemberId` 與會友關聯。

### 管理員會友管理
- 目標：供管理員管理教會會友資料及其與 LINE 使用者的綁定狀態。
- 輸入：無
- 輸出：管理員會友管理頁面。
- 頁面細節：
    - 會友表格，從最新到最舊排序（或按編號排序）。
        - 表格範例：
            | 會友編號 | 會友姓名 | 綁定狀態 | 操作 |
            |---|---|---|---|
            | 123456 | John Doe | 已綁定 (王小明) | 管理 |
            | 654321 | Jane Doe | 未綁定 | 管理 |
        - 點擊標頭時，表格將根據該標頭排序，再次點擊反轉順序。
        - 表格上方有一個搜尋區塊：
            - 搜尋姓名/編號。
    - 管理會友按鈕 (每列)。
        - 點擊按鈕時，將跳出彈窗，包含會友詳細資訊與編輯表單。
        - 彈窗內容：
            - **會友資訊區**：
                - 會友編號 (唯讀/可編輯)。
                - 會友姓名 (可編輯)。
            - **綁定資訊區** (唯讀)：
                - LINE 使用者名稱 (若已綁定)。
                - 綁定狀態顯示。
        - 彈窗操作按鈕：
            - **通過/駁回**：針對待審核的申請，提供通過或駁回選項。通過後兩端資料庫將同步紀錄綁定時間。
            - **儲存**：更新會友基本資料。
            - **解除綁定**：若該會友已綁定 LINE 帳號，提供解除綁定按鈕。
                - 點擊後跳出確認視窗。
                - 確認後，清除該會友與 LINE 使用者的連結，該 LINE 使用者將變為未綁定狀態。
            - **取消**：關閉視窗。

- 流程細節：
    - 使用者存取管理員會友管理頁面。
    - 管理員可檢視所有會友，無論是否綁定。
    - 對於已綁定的會友，可直接看到綁定的 LINE 使用者名稱，方便識別。
    - 可手動修正會友姓名或解除錯誤的綁定。

### 管理員奉獻類別管理
- 目標：供管理員新增或編輯奉獻類別（例如：建堂基金、感恩奉獻）。
- 輸入：無
- 輸出：奉獻類別管理頁面。
- 頁面細節：
    - 類別列表，顯示目前系統中所有的奉獻類別名稱與代碼。
    - 新增類別按鈕。
        - 點擊後跳出彈窗或表單，輸入「類別名稱」、「類別代碼（選填）」、「備註」。
        - 儲存按鈕。
    - 編輯類別按鈕。
    - 刪除類別按鈕（需檢查是否已有關聯的奉獻資料，若有則禁止刪除或僅能隱藏）。
