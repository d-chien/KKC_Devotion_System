# 功能規格書

## 使用者端

** 針對行動裝置體驗進行最佳化。 **

### 註冊
- 目標：如果使用者尚未註冊，使用 LINE 帳號進行註冊。
- 輸入：無
- 輸出：綁定結果。
- 流程細節：
    - 使用者存取註冊頁面。
    - 使用者點擊註冊按鈕。
    - 使用者被導向至 LINE 登入頁面。
    - 使用者使用 LINE 帳號登入。
    - 使用者被導向回註冊頁面。
    - 使用者點擊註冊按鈕。
    - 使用者被導向至首頁。
    - 使用者完成註冊。
- 備註：
    - 關於綁定流程，請參考 `./try_line_connect_page/main.py`。
    - 如果使用者已註冊，導向至首頁。
    - 如果使用者未登入，導向至 LINE 登入頁面。
    - 如果使用者已登入，導向至首頁。

### 登入
- 目標：如果使用者尚未登入，使用 LINE 帳號登入。
- 輸入：無
- 輸出：登入結果。
- 流程細節：
    - 使用者存取登入頁面。
    - 使用者點擊登入按鈕。
    - 使用者被導向至 LINE 登入頁面。
    - 使用者使用 LINE 帳號登入。
    - 使用者被導向至首頁。
    - 使用者已登入。
- 備註：
    - 關於登入流程，請參考 `./try_line_connect_page/main.py`。
    - 如果使用者已登入，導向至首頁。
    - 如果使用者未登入，導向至 LINE 登入頁面。

### 自動登入
- 目標：如果使用者處於已登入的工作階段，則導向至首頁。
- 輸入：無
- 輸出：登入結果。
- 流程細節：
    - 使用者存取登入頁面。
    - 使用者被導向至首頁。
    - 使用者已登入。
- 備註：
    - 關於自動登入流程，請參考 `./try_line_connect_page/main.py`。
    
### 首頁
- 目標：使用者奉獻紀錄的互動式儀表板。
- 輸入：無
- 輸出：使用者奉獻紀錄的儀表板。
- 頁面細節：
    - 標頭顯示姓名和會員 ID。
    - 副標題顯示資料時間範圍。
    - 強調文字顯示關鍵奉獻數據：
        - 總奉獻金額。
        - 總奉獻次數。
    - 甜甜圈圖顯示總奉獻金額的比例，按不同的奉獻類別分組。
        - 在圖表上顯示類別名稱。
        - 當使用者長按時強調該類別，顯示類別、總奉獻金額比例和奉獻金額。
    - 堆疊長條圖顯示奉獻金額，按不同的奉獻類別分組，數據按日期分組。
        - 在圖表上顯示日期。
        - 當使用者長按時強調該長條，顯示日期、總奉獻金額比例和奉獻金額。
    - 奉獻詳細資料表格，從最新到最舊排序。
        - 表格範例：
            | 日期 | 類別 | 金額 |
            |---|---|---|
            | 2025-01-01 | 月定奉獻 | 100 |
            | 2025-01-02 | 感恩奉獻 | 200 |
            | 2025-01-03 | 感恩節奉獻 | 300 |
        - 點擊標頭時，表格將根據該標頭排序，再次點擊反轉順序。
        - 表格上方有一個篩選區塊，包含 2 個篩選器：
            - 日期範圍篩選器。
            - 類別篩選器。
        - 每頁顯示 10 筆奉獻詳細資料，表格下方有一個分頁區塊，包含 2 個按鈕：
            - 上一頁按鈕。
            - 下一頁按鈕。
           
## 管理員端

** 針對桌面裝置體驗進行最佳化。 **

### 登入
- 目標：如果使用者尚未登入，使用帳號和密碼登入。
- 輸入：帳號、密碼
- 輸出：管理員儀表板。
- 流程細節：
    - 使用者存取管理員登入頁面。
    - 使用者輸入帳號和密碼，然後按下登入按鈕。
    - 使用者被導向至管理員儀表板。
- 備註：
    - 如果使用者已登入，導向至管理員儀表板。
    - 如果使用者未登入，導向至管理員登入頁面。

### 登出
- 目標：登出使用者。
- 輸入：無
- 輸出：登入頁面。
- 流程細節：
    - 使用者按下登出按鈕。
    - 使用者被導向至登入頁面。
    - 使用者已登出。

### 管理員標頭
- 目標：所有管理員頁面的管理員標頭。
- 輸入：無
- 輸出：導覽列。
- 頁面細節：
    - 標頭顯示教會名稱。
    - 副標題顯示目前時間。
    - 登出按鈕。
    - 功能導覽：
        - 儀表板
        - 上傳資料
        - 使用者管理
        - 教會管理（保留功能）

### 管理員儀表板
- 目標：供管理員管理使用者和奉獻資料的儀表板。
- 輸入：無
- 輸出：管理員儀表板。
- 頁面細節：
    - 甜甜圈圖顯示總奉獻金額的比例，按不同的奉獻類別分組。
        - 在圖表上顯示類別名稱。
        - 當使用者長按時強調該類別，顯示類別、總奉獻金額比例和奉獻金額。
    - 堆疊長條圖顯示奉獻金額，按不同的奉獻類別分組，數據按日期分組。
        - 在圖表上顯示日期。
        - 當使用者長按時強調該長條，顯示日期、總奉獻金額比例和奉獻金額。
    - 奉獻詳細資料表格，從最新到最舊排序。
        - 表格範例：
            | 日期 | 類別 | 金額 | 會友編號 | 會友姓名 |
            |---|---|---|---|---|
            | 2025-01-01 | 月定奉獻 | 100 | 123456 | John Doe |
            | 2025-01-02 | 感恩奉獻 | 200 | 123456 | John Doe |
            | 2025-01-03 | 感恩節奉獻 | 300 | 123456 | John Doe |
        - 點擊標頭時，表格將根據該標頭排序，再次點擊反轉順序。
        - 表格上方有一個篩選區塊，包含 4 個篩選器：
            - 日期範圍篩選器。
            - 類別篩選器。
            - 會友編號篩選器。
            - 會友姓名篩選器。
        - 每頁顯示 10 筆奉獻詳細資料，表格下方有一個分頁區塊，包含 2 個按鈕：
            - 上一頁按鈕。
            - 下一頁按鈕。
    
### 管理員上傳資料
- 目標：供管理員上傳奉獻資料。
- 輸入：無
- 輸出：管理員上傳資料頁面。
- 頁面細節：
    - 上傳區塊，用於上傳奉獻資料 Excel 檔案或 CSV 檔案。
    - 提交表單按鈕。
    - 取消表單按鈕。
- 流程細節：
    - 使用者存取管理員上傳資料頁面。
    - 使用者透過瀏覽檔案或拖放檔案上傳 Excel 檔案或 CSV 檔案。
    - 使用者按下提交按鈕。
    - 將跳出確認彈窗，詢問使用者是否確認上傳。
        - 如果使用者確認，系統將開始上傳資料。
        - 如果使用者取消，系統將導向至管理員上傳資料頁面。
    - 上傳成功後，將跳出成功彈窗，顯示已上傳多少筆記錄。按下確定按鈕導向至管理員儀表板。

### 管理員使用者管理
- 目標：供管理員管理使用者。
- 輸入：無
- 輸出：管理員使用者管理頁面。
- 頁面細節：
    - 使用者表格，從最新到最舊排序。
        - 表格範例：
            | 會友編號 | 會友姓名 | 會友已綁定 | 會友綁定日期 | 
            |---|---|---|---|
            | 123456 | John Doe | 是 | 2025-01-01 |
            | 654321 | Jane Doe | 否 | 2025-01-02 |
            | 111111 | John Doer | 是 | 2025-01-03 |
        - 點擊標頭時，表格將根據該標頭排序，再次點擊反轉順序。
        - 表格上方有一個篩選區塊，包含 4 個篩選器：
            - 會友編號篩選器。
            - 會友姓名篩選器。
            - 會友已綁定篩選器。
            - 會友綁定日期篩選器。
        - 每頁顯示 20 位使用者，表格下方有一個分頁區塊，包含 2 個按鈕：
            - 上一頁按鈕。
            - 下一頁按鈕。
    - 新增使用者按鈕。
        - 點擊按鈕時，將跳出彈窗，包含新增使用者的表單。
        - 表單有 2 個輸入：
            - 會員 ID 輸入。
            - 會員姓名輸入。
        - 提交表單按鈕。
        - 取消表單按鈕。
    - 編輯使用者按鈕。
        - 點擊按鈕時，將跳出彈窗，包含編輯使用者的表單。
        - 表單有 2 個輸入：
            - 會員 ID 輸入。
            - 會員姓名輸入。
        - 提交表單按鈕。
        - 取消表單按鈕。
    - 刪除使用者按鈕。
        - 點擊按鈕時，將跳出彈窗，詢問使用者是否確認刪除。
        - 如果使用者確認，系統將刪除該使用者。 ** 這需要與管理員再次確認。 **
        - 如果使用者取消，系統將導向至管理員使用者管理頁面。
- 流程細節：
    - 使用者存取管理員使用者管理頁面。
